def N := @ : *;
def O := @ : N;
def S := @ : /\(n:N).N;
def U := S O : N;

def Succ := \(x : N). S x : /\(x : N). N;

def Func := \(a : *).\(b : *)./\(x : a).b : /\(a : *)./\(b : *).*;
def Id := \(a : *).\(x : a).x : /\(a : *). /\(x : a).a;
def T := Succ : Func N N;
